# Tech Specs — Desktop

Status: Không có API/contract riêng cần đặc tả — desktop giao tiếp với backend qua contract chung tại `packages/contracts/` (từ Phase 2). Implementation reference: `apps/desktop/README.md`.
