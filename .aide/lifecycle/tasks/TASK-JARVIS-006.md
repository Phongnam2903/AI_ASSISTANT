---
key: TASK-JARVIS-006
story: STORY-JARVIS-006
status: blocked
blocked_by: [STORY-JARVIS-005, "Speech Pipeline implementation approval"]
---

# TASK-JARVIS-006 — Khóa `packages/contracts`

JSON Schema cho control envelope (`protocol_version`, `event_id`, `session_id`, `turn_id`, `sequence`, `type`, `payload`) và audio binary framing; generate type TypeScript + Python từ cùng schema.

## Bằng chứng

Schema file, generated types, test message sai schema bị từ chối.
