# packages/shared

Status: Placeholder. Intended for cross-cutting types/contracts shared between `apps/web` and `apps/api` (e.g., API request/response shapes) once there is real duplication to eliminate. Not populated in Phase 0 — introduced only when Phase 1 implementation shows concrete shared types worth extracting, per the project's YAGNI principle (see `/docs/architecture.md`).
